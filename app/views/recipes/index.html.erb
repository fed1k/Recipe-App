<p style="color: green"><%= notice %></p>

<div class="recipes_index_page">

  <div id="recipes">
    <% @recipes.each do |recipe| %>
      <%= render recipe %>
      <p>
        <%# <%= link_to "Show this recipe", recipe %>
      </p>
    <% end %>
  </div>

  <%= link_to "New recipe", new_recipe_path %>
</div>
