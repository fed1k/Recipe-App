<p style="color: green"><%= notice %></p>

<%= render @food %>

<div>
  <% if can? :update, @food%><%= link_to "Edit this food", edit_food_path(@food) %> | <%end%>
  <%= link_to "Back to foods", foods_path %>

  <% if can? :destroy, @food%><%= button_to "Destroy this food", @food, method: :delete %> | <%end%>
</div>
